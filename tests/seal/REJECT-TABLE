# The T-series of rejections, as data. One row per rejection the era claims.
# Format:  fixture | expected message fragment | row label
# The gate iterates this file, so adding a row means adding a line and a fixture
# — and a row with no fixture, or a fixture with no row, is a hard failure.
reject_expr|reject_expr.exl:3:13: error: expected expression|T1 a seal is a statement, not a value
reject_seam_enter|error: the seal seam is not callable directly|T2 sys::sys_seal_enter written by hand
reject_seam_exit|error: the seal seam is not callable directly|T3 sys::sys_seal_exit written by hand
reject_seam_extern|error: the seal seam is not declarable|T4 a raw extern re-declaring a seam symbol
reject_sealed_theft|belongs to resource 'Blitter', claimed by 'gfx'|S2 x seal a non-owner cannot seal its way in
reject_seal_escape_store|storing through a non-local pointer a value that embeds the address of a local binding|E1 escape rules reach INSIDE a region
reject_seal_escape_push|inserting into a non-local container an argument that embeds the address|E2 the same, on a second escape family
