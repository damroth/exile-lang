# Recogniser floors (FUZZ-SPEC 3.3 / Z1). Both directions on purpose: a filter's
# false POSITIVE eats budget silently, its false NEGATIVE drowns real findings in
# noise. Increment 1's regexes had one of each, which is why these exist.
# Format: fixture | expected recogniser ("none" = must NOT be filtered)
seam_call|kernel-era-superset
capability_decl|kernel-era-superset
enum_bar|none
closure_bar|none
mixed_bitwise|R7-mixed-bitwise-parens
multi_error|multi-error-pass-order
plain|none
arm_return|R12-arm-return
defer_return|none
