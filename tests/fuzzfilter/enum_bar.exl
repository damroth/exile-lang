// FALSE POSITIVE that Increment 1's parenthesis regex produced: `|` here is the
// enum-variant separator, not a bitwise operator. Filtering this spends budget
// on nothing and hides whatever the input would really have shown.
enum Colour { Red | Green | Blue }
fn main() { match Colour::Red { Colour::Red => println(1) | _o => println(2) } }
