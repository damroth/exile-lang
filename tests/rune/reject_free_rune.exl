// R6 (RUNE-SPEC §5, I-R3): a rune is foreign — never allocator-owned, never
// freed. It cannot stand where an `own *T` is expected.
fn drop_it(a: Allocator) {
    rune r: u32 at 0xDFF080 write;
    free(a, r);
}
fn main() { }
