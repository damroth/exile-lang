name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read
  packages: read

env:
  OPAMROOTISOK: "1"
  EXILE_TOOLCHAIN: /opt/amiga-gcc

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository_owner }}/exile-lang-ci:latest
      options: --user 0:0

    steps:
    - uses: actions/checkout@v4

    - name: Sync OCaml deps (incremental over baked switch)
      run: opam install . --deps-only --with-test --yes

    - name: Build compiler
      run: opam exec -- make build

    - name: Run tests
      run: opam exec -- make test

    - name: Build examples (compile to C + cc -ansi -pedantic -Wall)
      run: opam exec -- make examples

    - name: Verify host stdout snapshots
      run: opam exec -- make verify-host

    # ===== Self-host bring-up Faza −1 — differential-harness gate =====
    #
    # `make selfhost-diff` checks that the OCaml exilc still emits the
    # committed corpus byte-for-byte across all 66 examples × 3 dump
    # forms (tokens / AST / typed IR).  Any drift here is either an
    # intentional format change (the PR should also include the regen
    # under tests/golden/) or a regression in the OCaml emitter.
    #
    # The regen+git-diff combo below adds a second gate: it catches
    # cases where a PR changes the lexer / parser / typecheck output
    # but forgets to refresh tests/golden/.  Without this, a stale
    # corpus would silently mask the drift until somebody manually
    # ran `make selfhost-corpus`.
    - name: Verify selfhost dumps byte-stable (Faza -1 oracle)
      run: opam exec -- make selfhost-diff

    - name: Verify selfhost corpus is committed in sync
      run: |
        opam exec -- make selfhost-corpus
        git diff --exit-code tests/golden/ \
          || (echo "tests/golden/ drift — run 'make selfhost-corpus' \
                    and commit the refreshed files"; exit 1)

  amiga-verify:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository_owner }}/exile-lang-ci:latest
      options: --user 0:0

    steps:
    - uses: actions/checkout@v4

    - name: Sync OCaml deps (incremental over baked switch)
      run: opam install . --deps-only --with-test --yes

    - name: Build compiler
      run: opam exec -- make build

    - name: Cross-compile examples + verify under vamos
      run: opam exec -- make verify-amiga
