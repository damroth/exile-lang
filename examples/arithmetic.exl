fn main() {
    let sum     = 6 + 4;
    let diff    = 100 - 42;
    let product = 6 * 7;
    let quot    = 84 / 2;
    let mixed   = 1 + 2 * 3;

    println(sum);
    println(diff);
    println(product);
    println(quot);
    println(mixed);
}
