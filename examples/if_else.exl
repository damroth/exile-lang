fn abs(x: int) -> int {
    if x < 0 {
        return -x;
    } else {
        return x;
    }
}

fn classify(n: int) -> int {
    if n < 0 {
        return -1;
    } else if n == 0 {
        return 0;
    } else {
        return 1;
    }
}

fn main() {
    print(abs(-7));
    print(abs(3));

    print(classify(-5));
    print(classify(0));
    print(classify(42));
}
