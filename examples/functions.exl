fn add(a: int, b: int) -> int {
    return a + b;
}

fn square(n: int) -> int {
    return n * n;
}

fn power(base: int, exp: int) -> int {
    let mut result = 1;
    let mut i = 0;
    while i < exp {
        result = result * base;
        i = i + 1;
    }
    return result;
}

fn greet(name: str) {
    println(name);
}

fn main() {
    println(add(3, 4));
    println(square(5));
    println(power(2, 10));
    greet("hello from functions");
}
