// `match` takes an enum, an integer (incl. c_int), or a bool.  A struct has no
// tag and no finite domain to switch on.
struct P { x: int }
fn main() {
    let p = P { x: 1 };
    let r = match p { _o => 9 };
    println(r);
}
