// `&x` is a stack or field address; freeing it corrupts the allocator's
// bookkeeping.  Rejected on the SYNTAX, before types are even consulted.
fn main() {
    let a = default_allocator();
    let x = 5;
    free(a, &x);
    println(1);
}
