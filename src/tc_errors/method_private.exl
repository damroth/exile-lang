// A non-pub method is the impl's own business: callable only where the current
// `self` IS the target.
struct P { x: int }
impl P { fn m(self: *const P) -> int { return self.x; } }
fn main() {
    let p = P { x: 7 };
    println(p.m());
}
