// An array of bare `own *T` has no allocator at hand for its elements.
pub mod raw { extern fn make_c_allocator() -> Allocator; extern fn mk() -> own *int; }
fn main() { let arr: [own *int; 2] = [raw::mk(), raw::mk()]; println(1); }
